spring:
  application:
    name: configuredmicroservice1
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configurationserver
    consul:
      discovery:
        preferIpAddress: true
        instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
